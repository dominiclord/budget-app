fontSize = 14px

// Colors
// ======================================================
blue = #4c8ffe

// Screen widths
// ======================================================
fromTiny = 480px
toTiny = fromTiny - 1
fromSmall = 601px
toSmall = fromSmall - 1
fromMedium = 993px
toMedium = fromMedium - 1

// Components
// ======================================================
headerHeight = 60px

// Transitions
// ======================================================
speed = 0.3s
speedFast = (speed / 2)
easing = cubic-bezier(.55,0,.1,1)